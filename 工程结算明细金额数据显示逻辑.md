# 工程结算明细金额数据显示逻辑

## 一、数据结构

### 1. 费用列定义（FINANCE_COLUMNS）

共 10 个工程类费用列 + 1 个设备费用列：

| 列名 | 说明 |
|------|------|
| 工程硬装 | 硬装工程费用 |
| 广告物料 | 广告和物料费用 |
| 安装收尾 | 安装和收尾费用 |
| 家具 | 家具费用 |
| 灯具 | 灯具费用 |
| 配电箱 | 配电箱费用 |
| 设计 | 设计费用 |
| 空调 | 空调费用 |
| 消防 | 消防费用 |
| 其他工程类 | 其他工程类费用 |
| **设备费用** | 设备费用（独立列，不属于工程类费用小计） |

### 2. 结算行定义（FINANCE_ROWS）

共 16 行，分为 7 个金额类型：

| 金额类型 | 行明细 | rowId | 可编辑 | 公式 |
|---------|--------|-------|--------|------|
| **初始报价数据** | 初始报价金额 | `initQuote` | ✅ | - |
| | 初始成本金额 | `initCost` | ✅ | - |
| | 初始利润 | `initProfit` | ❌ | `quote - cost` |
| **增减项数据** | 增减项报价金额 | `changeQuote` | ✅ | - |
| | 增减项成本金额 | `changeCost` | ✅ | - |
| | 增减项利润 | `changeProfit` | ❌ | `quote - cost` |
| **佣金数据** | 佣金金额 | `commissionAmount` | ✅ | - |
| **最终结算数据** | 最终对外报价金额 | `finalQuote` | ❌ | `init + change` |
| | 最终应支付结算金额 | `finalPay` | ❌ | `initCost + changeCost - commissionAmount` |
| | 最终利润 | `finalProfit` | ❌ | `finalQuote - finalPay` |
| **实际支付数据** | 首次结算支付金额 | `firstPay` | ✅ | - |
| | 后续结算支付金额 | `secondPay` | ✅ | - |
| | 未支付金额 | `unpaid` | ❌ | `finalPay - firstPay - secondPay` |
| **平米单价数据** | 报价平米单价 | `unitQuotePrice` | ❌ | `finalQuote(工程硬装) / 面积` |
| | 成本平米单价 | `unitCostPrice` | ❌ | `finalPay(工程硬装) / 面积` |
| **毛利率数据** | 毛利率 | `grossMargin` | ❌ | `finalProfit / finalQuote` |

---

## 二、核心计算公式

### 1. 单元格值计算（calcFinanceValue）

```
initProfit      = initQuote - initCost
changeProfit    = changeQuote - changeCost
finalQuote      = initQuote + changeQuote
finalPay        = initCost + changeCost - commissionAmount
finalProfit     = finalQuote - finalPay
unpaid          = finalPay - firstPay - secondPay
unitQuotePrice  = finalQuote(工程硬装) / 门店面积
unitCostPrice   = finalPay(工程硬装) / 门店面积
grossMargin     = finalProfit / finalQuote
```

### 2. 汇总计算

| 汇总项 | 计算方法 |
|--------|---------|
| **工程类费用小计** | 10 个工程类费用列之和 |
| **整体筹建费用总计** | 工程类费用小计 + 设备费用 |

### 3. 毛利率特殊计算

毛利率在不同列有不同的计算方式：

| 列 | 计算方式 |
|---|---------|
| 工程类费用小计列 | `工程类费用小计(finalProfit) / 工程类费用小计(finalQuote)` |
| 整体筹建费用总计列 | `整体筹建费用总计(finalProfit) / 整体筹建费用总计(finalQuote)` |
| 设备费用列 | `设备费用(finalProfit) / 设备费用(finalQuote)` |
| 10个工程类费用列 | 各列单独计算：`本列(finalProfit) / 本列(finalQuote)` |

---

## 三、数据显示逻辑

### 1. 编辑面板（renderFinanceTable）

**可编辑单元格**：显示 `<input type="number">` 输入框
- 白色背景，可输入数值
- 无供应商时禁用（灰色背景）
- 失去焦点时自动保存

**公式单元格**：显示计算值
- 蓝色字体（`#1677ff`）
- 不可编辑

**特殊行显示**：

| 行类型 | 显示规则 |
|--------|---------|
| 单价行（isUnitPrice） | 只在**工程硬装**列显示数值，其他列为空 |
| 毛利率行（isPercent） | 所有列显示百分比（如 `15.50%`） |
| 普通行 | 所有列显示金额（保留2位小数） |

**汇总列显示**：

| 列 | 显示颜色 | 样式 |
|---|---------|------|
| 工程类费用小计 | 蓝色（`#1677ff`） | 加粗 |
| 设备费用 | 默认 | 普通 |
| 整体筹建费用总计 | 绿色（`#52c41a`） | 加粗 |

### 2. 列表页精简行（renderFinanceStoreList）

列表页只显示 **15 行数据中的 1 行**：**最终对外报价金额**（`finalQuote`）

```javascript
// 精简行只显示最终对外报价金额这一行的数据
html += `<td style="...">最终结算数据</td>`;  // 金额类型
html += `<td style="...">最终对外报价金额</td>`; // 类型明细
```

**精简行显示的列**：
- 10 个费用列：`calcFinanceValue(idx, 'finalQuote', col.key)`
- 工程类费用小计：`calcRowSubtotal(idx, 'finalQuote')`
- 设备费用：`calcFinanceValue(idx, 'finalQuote', '设备费用')`
- 整体筹建费用总计：`calcRowTotal(idx, 'finalQuote')`

### 3. 列表页展开行（renderFinanceTableRows）

点击门店行展开后，显示完整的 15 行明细数据，逻辑与编辑面板一致。

---

## 四、结算状态计算

### 1. 单列结算状态（calcColumnStatus）

| 状态 | 条件 |
|------|------|
| 报价阶段 | `finalPay > 0` 且 `firstPay = 0` 且 `secondPay = 0` |
| 已付首款 | `firstPay > 0` 且 `unpaid ≠ 0` |
| 已结算完成 | `firstPay > 0` 且 `unpaid = 0` |

### 2. 门店总结算状态（calcStoreTotalStatus）

遍历所有 10 个费用列，根据各列状态汇总：

| 状态 | 条件 |
|------|------|
| 报价阶段 | 有初始报价，但没有任何支付 |
| 已支付部分款项 | 有首次支付，但还有未支付金额 |
| 已结算完成 | 所有列的未支付金额都为 0，且有最终应支付 |

---

## 五、数据存储结构

每个门店的结算数据存储在 `store._finance` 中：

```javascript
store._finance = {
  // 行数据
  initQuote: { 
    '工程硬装': 10000, 
    '广告物料': 5000, 
    ...,
    _remark: '备注内容'
  },
  initCost: { '工程硬装': 8000, ... },
  changeQuote: { ... },
  changeCost: { ... },
  commissionAmount: { '工程硬装': 5000, ... },
  firstPay: { ... },
  secondPay: { ... },
  
  // 供应商配置
  _suppliers: {
    '工程硬装': '供应商A',
    '广告物料': '供应商B',
    ...
  }
}
```

**注意**：
- 可编辑行（initQuote, initCost, changeQuote, changeCost, firstPay, secondPay）存储原始输入值
- 公式行（initProfit, finalQuote, finalPay 等）**不存储**，实时计算
- 每行可有一个备注（`_remark`）

---

## 六、数据流向

```
用户输入 → store._finance[行][列] = 值
                ↓
        calcFinanceValue(行, 列)
                ↓
        公式计算（自动）
                ↓
        汇总计算（小计/总计）
                ↓
        渲染显示（编辑面板/列表页）
```

---

## 七、导出逻辑

导出 Excel 时：

1. 遍历所有门店
2. 每个门店生成 15 行数据
3. 每行包含：门店信息 + 10 个费用列 + 小计 + 设备费用 + 总计
4. 公式行按计算值导出
5. 毛利率和单价行按特殊格式导出

---

*文档生成时间：2026-05-14*
