# 2026-04-28 日志

## 今日工作

### 1. "筹建中门店状态"区块修复
- **筹建门店总数**：不再用 `STORES.length`（包含所有状态），改为只统计 6 个筹建中状态（选址中、设计中、施工前准备中、施工中、整改中、待开业）的总和，排除"已完成"和"已弃店"
- **新增"待开业"卡片**：金色图标 🎉，显示待开业门店数
- commit: `21075d7`

### 3. 百分比计算逻辑修改
- `calcProgress()` 从"最后一个打钩节点位置+1"改为"实际打钩节点数"
- 原逻辑中间跳过的节点会被误算进去，新逻辑只统计实际有数据的节点
- commit: `2e86577`

### 2. 状态筛选框加入"已完成"
- `initDashboardRegionFilters()` 中的 `statusOptions` 加入"已完成"选项
- 影响"区域门店及类型统计"和"门店周期统计"两个看板的状态下拉框
- commit: `c8151a3`
