# 2026-04-30 日志

## 今日工作

### 5. 施工阶段周期固定取两个节点
- 开始日期固定取"工程已进场"，结束日期固定取"门店已验收"
- "门店已验收"未填日期则该门店不计入统计
- 其他阶段保持原逻辑不变
- commit: `dae70e0`

### 4. 修改 calculatePhaseCycle 计算逻辑
- 原逻辑：收集阶段内所有日期排序后取最早最晚
- 新逻辑：按节点顺序找第一个和最后一个有日期的节点，计算两者天数差+1
- 中间跳过的节点不影响计算
- commit: `48a563e`

### 3. 所有日期节点取消最小日期限制
- 原逻辑：日期不能早于前面已填节点的日期
- 新逻辑：所有日期节点均可任意选择，不受前面节点约束
- commit: `ad8fa3f`

### 2. 停工节点已完成时可点击撤销
- 已完成状态下点击节点，弹出确认框询问是否撤销
- 撤销后清空停工天数，节点回到未完成状态
- commit: `221c1a8`

### 1. 停工天数输入0无法标记节点完成
- 原因：`confirmStopWorkDialog()` 中 `if (days === 0)` 会删除字段，导致节点未完成
- 修复：0 天也正常保存并标记节点完成（0 天=确认无停工，不同于未填）
- commit: `73dd0ce`
