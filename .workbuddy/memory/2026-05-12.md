# 2026-05-12 工作记录

## 已完成

### 1. 重构门店财务结算明细
基于用户上传的 Excel 模板 `/Users/fengshisan/Desktop/门店结算筹建系统模块.xlsx` 进行重构：

**新增基础信息固定列：**
- 门店名称、区域、门店类型、开业日期、门店面积
- 每行都显示（冻结效果），横向滚动时保持在左侧
- 开业日期自动引用"已正式开业"节点，面积自动引用门店面积字段

**新增3个公式行：**
- 硬装报价单价 = 硬装最终对外报价金额 ÷ 门店面积
- 硬装成本单价 = 硬装最终应支付结算金额 ÷ 门店面积
- 毛利率 = 最终利润 ÷ 最终对外报价金额
- 门店面积为0或空时，单价显示为空

**展示模式优化：**
- 基础信息列独立表头行，浅蓝底色
- 表格支持横向滚动（overflow-x: auto）
- 工程类费用小计、整体筹建费用总计保持蓝色/绿色高亮
- 编辑面板宽度从1100px扩大到1400px

**保留原有功能：**
- 供应商选择功能（编辑模式下选择供应商后解锁对应列输入）
- 单列结算状态（报价阶段/已付首款/已结算完成）
- 门店总结算状态标签
- 所有原有12行数据类型和公式逻辑

### 2. 修复编辑面板右侧露边问题
- **原因**：`financeEditPanel` 宽度从 1100px 扩大到 1400px 后，CSS 中 `right: -1200px` 未同步更新，导致面板无法完全隐藏到屏幕外
- **修复**：`right: -1200px` 改为 `right: -1450px`

### 3. 修复编辑面板输入后滚动位置重置问题
- **原因**：每次修改单元格都重新渲染整个表单（`bd.innerHTML = renderFinanceEditForm()`），导致 DOM 重建后焦点丢失，浏览器自动滚动到第一个可交互元素
- **第一次修复（无效）**：使用 `setTimeout` 和 `requestAnimationFrame` 延迟恢复 `scrollLeft`，但浏览器 focus 新元素时的自动滚动仍会覆盖
- **最终修复**：重写 `updateFinanceCell`，不再重新渲染整个表单。改为只更新受影响的单元格：
  - 新增 `refreshFinanceCells()` 函数，通过 `data-fin-cell`、`data-fin-subtotal`、`data-fin-total`、`data-fin-col-status`、`data-fin-store-status` 等 data 属性定位并更新 DOM
  - 更新的内容包括：公式行显示值、工程类费用小计、设备费用、整体筹建费用总计、单列结算状态、门店总结算状态
  - 供应商选择和备注编辑仍需重新渲染（涉及禁用状态变更），保留 `requestAnimationFrame` 恢复滚动

### 4. 结算明细数字格式和毛利率显示优化
- **所有数字统一两位小数**：去掉 `val !== 0 ? val.toFixed(2) : ''` 的条件判断，0值也显示 `0.00`
- **毛利率所有列显示百分比**：去掉 `col.key === '工程硬装'` 限制，10个费用列全部显示百分比（如 `50.00%`）
- **小计/总计列毛利率按本列利润÷报价计算**：
  - `calcFinanceValue` 中 `grossMargin` 增加对 `工程类费用小计` 和 `整体筹建费用总计` 的特殊处理
  - `calcRowSubtotal` / `calcRowTotal` 对 `grossMargin` 行做特殊处理，避免简单累加各列毛利率
  - 工程类费用小计毛利率 = 工程类费用小计最终利润 ÷ 工程类费用小计最终对外报价金额
  - 整体筹建费用总计毛利率 = 整体筹建费用总计最终利润 ÷ 整体筹建费用总计最终对外报价金额
- **设备费用无需供应商即可填写**：`updateFinanceCell` 中设备费用列跳过供应商检查

### 5. 结算明细表格合并单元格并居中
- **金额类型列合并**：按组合并单元格（初始报价数据 rowspan=3、增减项数据 rowspan=3、最终结算数据 rowspan=3、实际支付数据 rowspan=3、平米单价数据 rowspan=2、毛利率数据 rowspan=1），添加 `vertical-align:middle` 和 `text-align:center`
- **结算状态列合并**：整门店15行合并为1个单元格（rowspan=15），垂直居中显示门店总结算状态
- 动态计算 `typeRowSpans`，后续 FINANCE_ROWS 变更时自适应

### 6. 结算明细列表页改为统一大表格
- **重构 `renderFinanceStoreList`**：从卡片列表改为统一大表格
  - 顶部固定统一表头（23列：5基础信息 + 金额类型 + 类型明细 + 金额备注 + 10费用 + 小计 + 设备 + 总计 + 状态 + 操作）
  - 门店信息作为左侧固定列（sticky定位），依次向下排列
  - 每个门店默认只显示1行精简数据（最终对外报价金额）
  - 点击门店名称展开显示完整15行数据（内嵌 `renderFinanceTable`，CSS隐藏表头避免重复）
  - 编辑按钮移至操作列，保留原有功能
- **修改 `toggleFinanceDetail`**：在大表格中通过 `display:none/table-row` 切换展开行
- **新增CSS**：`.finance-detail-row .finance-table thead { display: none; }` 隐藏内嵌表格表头；`.finance-summary-row:hover` 增加hover效果

### 7. 结算明细列表页表头固定顶部
- 给列表页大表格所有表头 `<th>` 添加 `position:sticky;top:0;background:#f5f5f5;`
- 左侧固定列同时保留 `left` 值，横向纵向滚动均固定
- 非 sticky 的费用列、小计、总计、状态、操作列表头也添加 `sticky;top:0`，确保全部列标题行上下滚动时始终固定在顶部

**提交：** f5c6d4a, 84523d5, dd76cdf, 71f8c94, 7670719, a703b9a, a79001d, 6c709d8, 913b259 → GitHub Actions 自动部署中

### 8. 修复结算明细表格 sticky 固定列缝隙问题
- **原因**：表格 `min-width:1400px` 不足以容纳所有列，浏览器压缩列宽后，sticky 列的固定 `left` 值与实际渲染宽度不匹配，产生缝隙，底层滚动内容从缝隙中透出
- **修复**：将相邻 sticky 列的 `left` 值统一减小 2px，使列之间产生微小重叠，覆盖缝隙
- **影响范围**：列表页表头/精简行、编辑面板表头/数据行（区域/门店类型/开业日期/门店面积/金额类型/类型明细/金额备注等列）

### 9. 修复结算明细列表页表头真正固定在视口顶部
- **原因**：表头在 `overflow-x:auto` 容器内，`position:sticky;top:0` 只能相对于该容器定位。页面上下滚动时，容器移出视口，表头跟着一起移出，导致看起来不固定
- **修复**：采用双表格方案
  - 表头独立放在 `overflow-x` 容器外，用 `position:sticky;top:48px` 相对于视口固定（导航栏下方）
  - 表格内容放在 `overflow-x:auto` 容器内，可横向滚动
  - 两个表格使用 `table-layout:fixed` + `<colgroup>` 确保列宽严格一致
  - JS 监听内容容器 `scroll` 事件，同步滚动固定表头
- **效果**：上下滚动时表头始终固定在视口顶部；横向滚动时固定表头同步跟随内容滚动

**提交：** f5c6d4a, 84523d5, dd76cdf, 71f8c94, 7670719, a703b9a, a79001d, 6c709d8, 913b259, 2f57817, 0e981a3 → GitHub Actions 自动部署中

### 10. 结算明细列表页添加表头筛选功能
- **筛选控件行**：在固定表头下方新增筛选行，同样 `position:sticky` 固定在顶部（导航栏下方）
- **筛选控件**：
  - 门店名称：文本输入搜索
  - 区域：多选下拉（`<select multiple>`，按住 Ctrl/Cmd 多选）
  - 门店类型：单选下拉
  - 开业日期：开始日期 + 结束日期区间
  - 门店面积：最小值 + 最大值数字区间
  - 金额类型：多选下拉
  - 类型明细：多选下拉
  - 结算状态：单选下拉（报价阶段 / 已支付部分款项 / 已结算完成）
- **交互**：点击"应用"执行筛选，点击"重置"清空恢复默认视图
- **行级筛选**：金额类型/类型明细筛选自动展开匹配的门店，只显示匹配的行
- **无匹配提示**：筛选结果为空时显示"无匹配数据"
- **renderFinanceTable**：新增 `rowFilter` 参数，过滤后重新计算合并单元格（rowspan）

### 11. 结算明细筛选控件统一为自定义下拉样式
- **问题**：区域、门店类型、开业日期、门店面积、金额类型、类型明细的筛选控件使用原生 `<select>` / `<input>`，样式与结算状态的自定义下拉不匹配
- **修复**：
  - **区域 / 金额类型 / 类型明细**：多选自定义下拉（`position:relative` 容器 + `filter-btn` 按钮 + 下拉 div，选项带复选框）
  - **门店类型 / 结算状态**：单选自定义下拉（`.dropdown-item` 样式，选中项蓝色高亮）
  - **开业日期 / 门店面积**：美化输入框（圆角边框 `#d0d0d0`、`border-radius:20px`，与整体风格统一）
  - **应用/重置按钮**：改为圆角胶囊样式
- **新增JS函数**：`toggleFinAreaDropdown`、`refreshFinAreaDropdown`、`updateFinAreaBtn` 等区域/类型/金额/明细/状态各 5 组交互函数
- **全局状态变量**：`finFilterAreas`、`finFilterType`、`finFilterAmountTypes`、`finFilterDetails`、`finFilterStatus`
- **修改 `applyFinanceFilter`**：读取自定义下拉的选中值
- **修改 `resetFinanceFilter`**：重置自定义下拉状态并更新按钮文字
- **点击外部关闭**：`document.addEventListener('click')` 检测点击位置，自动关闭所有结算明细下拉

**提交：** f5c6d4a, 84523d5, dd76cdf, 71f8c94, 7670719, a703b9a, a79001d, 6c709d8, 913b259, 2f57817, 0e981a3, 83f94de → GitHub Actions 自动部署中

### 12. 门店面积筛选改为弹窗填写样式
- **问题**：门店面积筛选使用两个内联输入框，Chrome 密码管理器反复干扰（多次修复无效），且用户无法感知输入是否成功
- **修复**：将内联输入框替换为弹窗填写样式
  - 筛选行面积列改为按钮 `全部 ▼`，点击打开弹窗
  - 新增弹窗 HTML：`finAreaOverlay` + `finAreaModal`，与停工天数弹窗共用相同样式
  - 弹窗内两个数字输入框：最小面积、最大面积，支持回车确认
  - 新增状态变量：`finFilterAreaMin`、`finFilterAreaMax`
  - 新增函数：`openFinAreaModal`、`closeFinAreaModal`、`confirmFinAreaModal`、`updateFinAreaRangeBtn`
  - 修改 `applyFinanceFilter`：从状态变量读取面积值
  - 修改 `resetFinanceFilter`：重置状态变量并更新按钮文字为"全部 ▼"
  - 按钮文字动态显示：`≥ min ㎡`、`≤ max ㎡`、`min - max ㎡`

### 13. 结算明细表头布局错乱修复（进行中）
- **问题**：双表格方案下，表头标题和内容数字滚动不同步、显示范围不一致、表头超出主界面
- **修复尝试1**：去掉表头 div 的 `overflow-x:hidden` → 导致表头撑破主界面，下拉菜单显示正常
- **修复尝试2**：恢复 `overflow-x:hidden`，下拉菜单被裁剪 → 无法使用
- **最终方案**：下拉菜单打开时临时将表头 div `overflow-x` 设为 `visible`，关闭后恢复 `hidden`
  - 修改 `closeAllFinDropdowns`：关闭时恢复 `overflow-x: hidden`
  - 修改 5 个 `toggleFin*Dropdown`：打开时临时设为 `overflow-x: visible`
  - 这样表头平时被限制在主界面内，下拉打开时临时允许溢出
- **列宽调整**：结算状态 90px→110px，操作列 70px→110px，确保文字和按钮完整显示

**提交：** cb3b164, 2bf4177, 46684ae, ad787b9 → GitHub Actions 自动部署中

### 14. 修复编辑面板被表头覆盖
- **原因**：表头 div `z-index:300` 高于编辑面板 `z-index:203`，导致编辑面板弹出时被表头覆盖
- **修复**：表头 div z-index 从 300 降到 200，编辑面板保持 203，确保编辑面板始终在最上层

### 15. 结算明细行级筛选优化：隐藏默认精简行，直接插入主表格
- **问题**：行级筛选（金额类型/类型明细）时，默认精简行（黄框）仍然显示，且嵌套表格导致列与表头不对齐
- **修复**：
  - 新增 `renderFinanceTableRows(storeIdx, rowFilter)` 函数：只渲染 `<tr>` 行（非完整表格），列结构与主表格完全一致
  - 修改 `applyFinanceFilter`：当行级筛选激活时
    - 隐藏精简行（`finance-summary-row`）
    - 清除之前插入的筛选行
    - 调用 `renderFinanceTableRows` 生成匹配行，通过 `insertAdjacentHTML('afterend')` 直接插入主表格 tbody
    - 门店信息列（前5列）使用 rowspan 合并，覆盖所有匹配行
    - 结算状态列和操作列同样使用 rowspan 合并
  - 修改 `resetFinanceFilter`：清除所有 `.finance-filter-row` 后再恢复精简行显示
  - 修复结算状态 rowspan：从固定 `FINANCE_ROWS.length` 改为动态 `rowCount`（匹配行数）
- **效果**：筛选后只显示匹配行，无嵌套表格，列与表头严格对齐

**提交：** 6097a0a → GitHub Actions 自动部署中

### 16. 结算明细表头左侧8列左右固定
- **问题**：表头区域只有上下固定，没有左右固定，导致左右滑动时表头左侧列不冻结
- **修复**：
  - 修改表头 div 结构：`#finance-list-header` 内新增 `#finance-list-header-scroll`（`overflow-x:auto;width:100%`），两个 table 放入其中
  - 去掉 `#finance-list-header` 的 `overflow-x:hidden`（改到内层 div）
  - 标题行左侧8列 `<th>` 添加 `position:sticky;left:X;background:#f5f5f5;z-index:3`
  - 筛选行左侧8列 `<td>` 添加 `position:sticky;left:X;background:#fff;z-index:3`
  - 右侧费用列 `<th>` 添加 `position:sticky;top:0;background:#f5f5f5;z-index:2`（上下固定）
  - 更新 JS 同步滚动：`headerDiv` 改为 `headerScroll`，添加双向同步（内容滚动同步表头，表头滚动同步内容）
  - 更新下拉菜单 overflow 切换逻辑：操作对象从 `#finance-list-header` 改为 `#finance-list-header-scroll`
- **效果**：左右滑动时，表头左侧8列（门店名称~金额备注）和数据区一样左右冻结固定

**提交：** 130daa1 → GitHub Actions 自动部署中

### 17. 门店展开明细直接插入主表格，列与表头严格对齐
- **问题**：点击门店展开15行明细时，使用独立嵌套表格 `<table>`，没有 `table-layout:fixed` 和 `<colgroup>`，导致列宽与主表格不一致，列与表头不对齐
- **修复**：
  - `renderFinanceTableRows` 增加第三个参数 `rowClass`，支持传入自定义 class（展开时用 `finance-expand-row`，筛选时用 `finance-filter-row`）
  - 重写 `toggleFinanceDetail`：
    - 展开时：隐藏精简行，调用 `renderFinanceTableRows(storeIdx, null, 'finance-expand-row')` 生成15行明细，通过 `insertAdjacentHTML('afterend')` 直接插入主表格 tbody
    - 折叠时：删除所有 `finance-expand-row`，恢复精简行显示
  - 修改 `applyFinanceFilter`：筛选前清理该门店的 `finance-expand-row` 和 `finance-filter-row`
  - 修改 `resetFinanceFilter`：清除所有 `.finance-filter-row` 和 `.finance-expand-row`
- **效果**：展开后的15行明细与主表格共用同一套 `colgroup` 列宽定义，列与表头严格对齐；左侧8列 sticky 左右固定同样生效

**提交：** f74285a → GitHub Actions 自动部署中
