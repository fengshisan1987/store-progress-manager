server {
    listen 80;
    server_name 47.114.120.73;
    
    # 前端静态文件
    location / {
        root /var/www/store-progress-manager;
        index 进度管理系统.html;
        try_files $uri $uri/ =404;
    }
    
    # 后端 API 反向代理 - 支持端口范围 3001-3005
    location /api/ {
        proxy_pass http://localhost:3001/api/;
        error_page 502 503 504 = @fallback_api;
    }
    
    location @fallback_api {
        proxy_pass http://localhost:3002/api/;
        error_page 502 503 504 = @fallback_api2;
    }
    
    location @fallback_api2 {
        proxy_pass http://localhost:3003/api/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
